/** Auth DTO types now live in @twy/core (shared with the mobile app). Re-exported for existing imports. */
export type {
  User,
  AuthResponse,
  RegisterInput,
  RegistrationResult,
  LoginInput,
  OtpPurpose,
} from "@twy/core";
