package com.twelveweekyear.feature.dashboard.dto;

import java.util.UUID;

/** A habit as shown on the sprint dashboard: streak, today's state, and this-sprint consistency. */
public record DashboardHabit(
        UUID id,
        String name,
        int currentStreak,
        boolean completedToday,
        int sprintCompletionRate
) {
}
