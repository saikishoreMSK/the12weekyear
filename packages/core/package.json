{
  "name": "@twy/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared core for The 12 Week Year — API client, envelope types, and (later) query hooks, zod schemas, and the ported calculators. Consumed by @twy/web and @twy/mobile.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@tanstack/react-query": "^5"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.101.2",
    "typescript": "^5"
  }
}
